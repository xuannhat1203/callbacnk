function check(array, number) {
    for (let i = 0; i < array.length; i++) {
        if (array[i] % 2 === 0) {
            console.log(array[i]);
        }else {
            console.log(array[i]);
        }
    }
} 
console.log(check([1, 2, 3, 4, 5, 6]),(element) => element %2==0);